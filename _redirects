# Institute for Lebanon -- Cloudflare Pages
# Cloudflare Pages serves archive.html at /archive automatically (Clean URLs).
# No redirect rules needed -- removing them prevents redirect loops.

# Redirect legacy hash URLs to proper pages
/index.html     /     301
